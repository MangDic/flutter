import 'package:flutter/material.dart';

class lottoBallColor extends CustomPainter{

  lottoBallColor({required this.isFinish});

  final bool isFinish;

  @override
  void paint(Canvas canvas, Size size) {

  }

  @override
  bool shouldRepaint(covariant CustomPainter oldDelegate) {

    return false;
  }
}